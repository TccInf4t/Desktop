package br.com.onpecas.view;

import java.io.IOException;

import br.com.onpecas.controller.*;
import javafx.fxml.FXMLLoader;
import javafx.scene.Scene;
import javafx.scene.control.ScrollPane;
import javafx.scene.layout.AnchorPane;
import javafx.scene.layout.BorderPane;
import javafx.stage.Stage;

public class CallScene {
	BorderPane border;

	public CallScene() {
		// TODO Auto-generated constructor stub
		this.border = CSOPControllerExpedicao.border;
		
		System.out.println("ta no cons");
	}

	// Carrega a tela de cadastro de transportadora
	public void loadTransportadoraDetalhe(){
		FXMLLoader loader = new FXMLLoader();
		loader.setLocation(getClass().getResource("TransportadoraDetalhe.fxml"));

		try {
			AnchorPane anchor = (AnchorPane) loader.load();
			border.setCenter(anchor);
		} catch (IOException e) { e.printStackTrace(); }
	}

	// Carrega a tela de lista de transportadora
	public void loadTransportadoraInicial(){
		FXMLLoader loader = new FXMLLoader();
		loader.setLocation(getClass().getResource("TransportadoraInicial.fxml"));

		try {
			ScrollPane scroll =(ScrollPane) loader.load();
			
			border.setCenter(scroll);
		} catch (IOException e) { e.printStackTrace(); }
	}

	public void LoadMain() throws IOException{

		FXMLLoader loader = new FXMLLoader();
        loader.setLocation(getClass().getResource("UsuariosGrupos.fxml"));
        loader.setController(new PainelPrincipalController());

		ScrollPane module = (ScrollPane) loader.load();
		border.setCenter(module);
	}

	//Esse método serve para carregar a estrutura de borda do sistema
	public void LoadBorder(Stage primaryStage) throws IOException{

		FXMLLoader loader = new FXMLLoader();
        loader.setLocation(getClass().getResource("PainelPrincipal.fxml"));
        loader.setController(new PainelPrincipalController());
		border = (BorderPane) loader.load();
        Scene scene = new Scene(border);

        primaryStage.setScene(scene);
        primaryStage.show();

	}
}
